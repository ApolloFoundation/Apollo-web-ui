include includes/mixins

doctype html
html(lang="en")
	+head('APL')
	body(data-content-area="1400px")
		//+modalWindow('sendMoney')
		//+modalWindow('sendMoneyPrivate')
		+menuBar()
		.site-content
			.page-content
				.page-header
					.container-fluid
						.row
							.col-md-6
								.page-title-box
									.page-title-box
										p.title Scheduled transactions
										.breadcrumbs
											a Apollo Wallet /
											strong: a Scheduled transactions
							.col-md-6
								.user-search-box
									.search-bar
										input(type="text")
									.user-box
										.user-name
											a Viktoria Apollo
										.user-avatar(style="background-image: url(../img/man.svg)")
				.page-body.container-fluid
					.scheduled-transactions
						.info-box.info
							p Incorrect "adminPassword" (locked for 1 hour, too many incorrect password attempts)
						.approval-request.white-space
							.alert No current approval requests.
